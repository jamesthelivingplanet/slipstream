## Why

`initFromBackend().then(...)` had no `.catch`. Inside it, `Promise.all([listRepos(), listTickets()])` coupled the two fetches: if the ticket provider errored (bad Linear/Jira credentials, provider down) or the daemon wasn't up yet, the whole init rejected — `repos` was never set, and `MissionControl.svelte:165` (`showOnboarding = $repos.length === 0`) showed **"Add a repository to get started"** to a user who has repos and running sessions. No toast, no retry — the failure was invisible and the message actively wrong.

Also, loading all tickets at once could be slow for users with many tickets. Added pagination + search + Nulliel loaders for better UX.

## What changed

### Backend (pagination + error handling)
- **electron/tickets/compositeProvider.ts**: `listTickets` now accepts `{ page?, pageSize?, query? }` and returns `PaginatedTickets`
- **electron/tickets/linearProvider.ts**: Added GraphQL pagination support with `first`, `after`, `term` variables; returns `PaginatedTickets`
- **electron/tickets/jiraProvider.ts**: Added JQL-based search + pagination; returns `PaginatedTickets`
- **electron/tickets/emptyProvider.ts**: Updated to return empty `PaginatedTickets`
- **electron/shared/contract.ts**: Added `PaginatedTickets` interface and updated `ITicketProvider.listTickets` + `SlipstreamApi.listTickets`
- **electron/core/rpc.ts**: Passes pagination options through to ticket providers

### Frontend (stores + UI)
- **src/lib/stores.ts**: 
  - Added `ticketsLoading`, `ticketsTotalCount`, `ticketsPage`, `ticketsPageSize`, `ticketsHasMore`, `ticketsQuery` stores
  - `initFromBackend` now fetches repos/tickets/sessions independently with per-call try/catch + error toasts
  - `refreshTickets` / `loadMoreTickets` / `setTicketsQuery` for pagination control
  - `refreshAndReconcile` fetches first page (100) for reconciliation
- **src/lib/ipc.ts**: Updated `listTickets` signature to accept pagination options
- **src/lib/wsApi.ts**: Updated web socket API client
- **src/lib/components/MissionControl.svelte**: 
  - Added NullielLoader while loading tickets
  - Added search input for tickets
  - Added "Load more" button with pagination
  - Shows total count in header
- **src/lib/components/NewAgentDialog.svelte**: Same UX improvements for ticket picker
- **src/lib/components/MissionControl.svelte**: Added styling for loading/error states

### Fixes
- Fixed Jira provider `hasMore` logic (use `!!` for truthy check)
- Updated all tests to work with `PaginatedTickets` return type

## Acceptance criteria

- [x] Failed repo/ticket/session fetch shows error toast (not silent)
- [x] Failed load shows retry UI in Mission Control (not onboarding)
- [x] Genuinely empty state (no repos, no error) still shows onboarding
- [x] Retry button re-fetches all data
- [x] Tickets load with Nulliel spinner in Mission Control + New Agent Dialog
- [x] Search filters tickets client-side (with server-side where supported)
- [x] Pagination loads more tickets incrementally
- [x] Lint, typecheck, and tests pass