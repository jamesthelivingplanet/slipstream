node_modules/

# This repo uses pnpm (pnpm-lock.yaml) — a stray `npm install` shouldn't
# silently reintroduce a second, unmaintained lockfile.
package-lock.json

dist/
dist-electron/
dist-apk/
out/
release/
.pnpm-store/
.DS_Store

# vitest coverage reports (generated by `pnpm test:coverage`)
/coverage

# pod deploy secrets (docker compose reads .env)
.env
.claude/tdd-guard/data/
